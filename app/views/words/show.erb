<h1>Individual word page for the word "<%= "#{@word.text}" %>"</h1>
<table class="table table-striped">
	<thead>
		<th>Word</th>
		<th>Text</th>
		<th>Letters</th>
	</thead>
	<tbody>
		<tr>
			<td><%= "#{@word.id}" %></td>
			<td><%= "#{@word.text}" %></td>
			<td><%= "#{@word.letters}" %></td>
		</tr>
	</tbody>
</table>
<div class="back-button">
	<a href="/words"><button type="submit" class="btn">Back to Word List</button></a>
</div>